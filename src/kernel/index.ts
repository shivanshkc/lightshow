// v2 kernel (application core) — placeholder.
//
// In v2, the kernel owns state transitions and exposes:
// - Commands (write API)
// - Queries (read API)
// - Events (minimal notifications)
//
// Implemented in Milestones 03+; this file exists now to establish the public API.

export * from './Kernel';


